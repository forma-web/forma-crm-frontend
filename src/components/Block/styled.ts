import { styled } from '@mui/material';

import { BlockBaseStyled } from '../../styles/containers';
import { INDENTS } from '../../styles/size';

export const BlockStyled = styled(BlockBaseStyled)`
  padding: ${INDENTS.paddingBlockBase};
`;
